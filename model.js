const DEFAULTS={setpoint:1.5,geo:.65,geoTemp:80,csp:3.2,cspTemp:150,capacity:12,initial:7,efficiency:.9};
const PROFILE=[0,0,0,0,0,0,0,.828,1.6,2.263,2.771,3.091,3.2,3.091,2.771,2.263,1.6,.828,0,0,0,0,0,0,0];
const GEOTEMP=[80,80.1,80.2,80.3,80.4,80.5,80.5,80.5,80.5,80.4,80.3,80.2,80.1,79.9,79.8,79.7,79.6,79.6,79.5,79.5,79.5,79.6,79.6,79.7,79.9];
function simulate(p){let energy=p.initial;return PROFILE.map((solar,h)=>{const dni=Math.max(0,Math.sin(Math.PI*(h-6)/12));const csp=solar/3.2*p.csp,raw=p.geo+csp,start=energy;let charge=0,discharge=0;if(raw>p.setpoint)charge=Math.min(raw-p.setpoint,(p.capacity-energy)/p.efficiency);else discharge=Math.min(p.setpoint-raw,energy*p.efficiency);energy=Math.max(0,Math.min(p.capacity,energy+charge*p.efficiency-discharge/p.efficiency));const exported=Math.min(p.setpoint,raw+discharge),curtailed=Math.max(0,raw-exported-charge),stability=Math.max(0,100*(1-Math.abs(exported-p.setpoint)/p.setpoint));return{h,dni:Math.round(dni*1000)/1000,geo:p.geo,csp,raw,charge,discharge,exported,curtailed,start,energy,soc:energy/p.capacity*100,stability,setpoint:p.setpoint,geoTemp:GEOTEMP[h]-80+p.geoTemp,cspTemp:p.geoTemp+(p.cspTemp-p.geoTemp)*dni,mode:charge>1e-8?'شحن التخزين':discharge>1e-8?'تفريغ التخزين':curtailed>1e-8?'مستقر · خفض الفائض':exported<p.setpoint-1e-8?'عجز · التخزين فارغ':'الوضع المستقر'};});}
if(typeof module!=='undefined')module.exports={DEFAULTS,simulate};
